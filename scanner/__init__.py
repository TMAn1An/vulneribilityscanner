"""A small, proxy-aware web vulnerability scanner.

Designed so that all HTTP traffic can be routed through an upstream proxy
such as Burp Suite, letting you observe, intercept, and manually test every
request the scanner makes in Burp's Proxy > HTTP history.
"""

__version__ = "0.1.0"
